name: mean_intensity
channels:
  - conda-forge
  - defaults
dependencies:
  - dask
  - scikit-image
  - numpy
  - xarray
  - zarr
  - pytorch
  - torchvision
  - cudatoolkit=11.3
  - joblib
  - dask-jobqueue